# Backend Configuration
DATABASE_URL=sqlite:///./status_dashboard.db
LOG_LEVEL=INFO
DEFAULT_POLL_FREQUENCY=300
MAX_CONCURRENT_SCRAPES=5
REQUEST_TIMEOUT=30

# CORS (comma-separated origins)
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# User Agent
USER_AGENT=StatusDashboard/1.0 (+https://github.com/yourusername/status-dashboard)

# Frontend Configuration (for Vite)
VITE_API_URL=http://localhost:8000/api

# Email Notifications
# Configure these to enable email alerts when services change status
# Leave blank to disable email notifications

# SMTP server settings (e.g., Gmail, SendGrid, AWS SES)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Email addresses
SMTP_FROM_EMAIL=status-dashboard@your-domain.com
NOTIFICATION_EMAIL=you@your-domain.com

# Notification settings
NOTIFICATION_COOLDOWN_MINUTES=60  # Minimum time between notifications per service (prevents spam)

# Example Gmail configuration:
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an "App Password" at https://myaccount.google.com/apppasswords
# 3. Use these settings:
#    SMTP_HOST=smtp.gmail.com
#    SMTP_PORT=587
#    SMTP_USERNAME=your-email@gmail.com
#    SMTP_PASSWORD=your-app-password (16-character password from step 2)
#    SMTP_FROM_EMAIL=your-email@gmail.com
#    NOTIFICATION_EMAIL=your-email@gmail.com

# Notification behavior:
# - Sends email when a service goes from OPERATIONAL to any other status
# - Sends email when a service returns to OPERATIONAL after being down
# - Respects cooldown period to prevent email spam
# - Only sends one notification per status change
